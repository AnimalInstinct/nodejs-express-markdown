{
  "dependencies": {
    "markdown-it": "^13.0.1"
  },
  "devDependencies": {
    "@types/node": "^18.7.16",
    "typescript": "^4.8.3"
  },
  "scripts": {
    "start": "node ./build/app.js",
    "build": "tsc -p tsconfig.json",
    "build:watch": "tsc -w -p tsconfig.json"
  }
}